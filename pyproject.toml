[project]
name = "mystuff"
version = "1.0.0"
description = "My Stuff API - file, note, media management and AI services"
requires-python = ">=3.13,<3.14"

dependencies = [
    # API
    "fastapi==0.136.1",
    "uvicorn[standard]==0.48.0",
    # Database
    "sqlalchemy==2.0.49",
    "alembic==1.18.4",
    "psycopg2-binary==2.9.12",
    # Validation / Configuration
    "pydantic==2.13.4",
    "pydantic-settings==2.14.1",
    # Authentication / Security
    "python-jose==3.5.0",
    "bcrypt==5.0.0",
    "email-validator==2.3.0",
    # Uploads / Forms
    "python-multipart==0.0.29",
    "aiofiles",
    # Email
    "aiosmtplib==5.1.0",
    # Storage
    "boto3==1.43.51",
    # Documents / Media
    "python-docx==1.2.0",
    "fpdf2==2.8.7",
    "pillow==12.3.0",
    # Networking
    "httpx==0.28.1",
    "websockets==16.0",
    # Environment
    "python-dotenv==1.2.2",
    "huggingface-hub>=1.16.4",
]

[dependency-groups]
dev = [
    "ruff==0.15.14",
    "pyright==1.1.409",
    "black==26.5.1",
    "autopep8==2.3.2",
    "taskipy==1.14.1",
    "boto3-stubs[s3]>=1.43.52",
    "pylint>=4.0.6",
]

ai = [
    "numpy==2.4.6",
    "torch==2.12.0",
    "torchaudio",
    "torchvision",
    "transformers==5.9.0",
    "safetensors==0.7.0",
    "onnxruntime==1.24.4",
    "sherpa-onnx==1.13.2",
]

# --- UV PyTorch CPU Index Routing ---
[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = [{ index = "pytorch-cpu" }]
torchaudio = [{ index = "pytorch-cpu" }]
torchvision = [{ index = "pytorch-cpu" }]

# --- Tool Settings ---
[tool.ruff]
line-length = 200
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["B008"]

[tool.ruff.format]
quote-style = "double"

[tool.pylint."messages_control"]
disable = [
    # Docstring checks
    "missing-module-docstring",
    "missing-class-docstring",
    "missing-function-docstring",
    # Similar lines / boilerplate checks (common in SQLAlchemy models/repos)
    "duplicate-code",
]

[tool.pyright]
typeCheckingMode = "strict"
venvPath = "."
venv = ".venv"

[tool.taskipy.tasks]
dev = """
export LD_LIBRARY_PATH=$PWD/.venv/lib/python3.13/site-packages/onnxruntime/capi:$LD_LIBRARY_PATH && \
fuser -k 8000/tcp 8001/tcp 8002/tcp 8003/tcp 8004/tcp 8005/tcp || true && \
clear && \
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
"""
mm = "uv run alembic revision --autogenerate -m"
migrate = "uv run alembic upgrade head"
migrate_prod = "ENVIRONMENT=production DEBUG=false DEMO_MODE=false DATABASE_URL='postgresql://neondb_owner:npg_KV3XQsD8PWdH@ep-weathered-meadow-za06ickq-pooler.c-2.eu-west-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require' SECRET_KEY='OoprAVGnRFk9ui77CEIo8H-XQJV3BOFW8WmZNLpkGwc' uv run alembic upgrade head"
push = "bash scripts/db-push.sh"
remove_stale = """
find . -type d -name "__pycache__" -exec rm -rf {} + &&
find . -type f -name "*.pyc" -delete
"""

[tool.pylint.design]
min-public-methods = 0

[tool.ruff.lint.per-file-ignores]
"app/database/migrations/env.py" = ["E402", "I001"]